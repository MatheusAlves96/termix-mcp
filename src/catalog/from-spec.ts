// Placeholder until Phase 2 (spec-driven generation) lands: `npm run spec:generate`
// will populate src/generated/operations.ts, and this file will turn each
// operation into a ToolSpec via src/catalog/overrides.ts.
import type { ToolSpec } from "./types.js";

export const generatedCatalog: ToolSpec[] = [];
